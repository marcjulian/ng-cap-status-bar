import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ng-cap-status-bar',
  webDir: 'dist/ng-cap-status-bar',
  bundledWebRuntime: false
};

export default config;
